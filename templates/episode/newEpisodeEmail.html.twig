{% extends 'layout.html.twig' %}

{% block body %}
	<h1>New episode
		{{ episode.title }}
		created</h1>
	<p>Hello, the new episode
		{{ episode.title }}
		{{ episode.number }}
		has been created on Wild Series</p>
	<p>Série :
		{{ program.title }}
		saison
		{{ season.number }}</p>
	<p>Synopsis :
		{{ episode.synopsis }}</p>
	<a href="{{ absolute_url(path('app_episode_show', {'slug' : episode.slug})) }}">Watch it !</a>
{% endblock %}
